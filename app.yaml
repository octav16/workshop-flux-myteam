apiVersion: apps/v1
kind: Deployment
metadata:
  name: myteam-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myteam-app
  template:
    metadata:
      labels:
        app: myteam-app
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: myteam-app-service
  namespace: default
spec:
  selector:
    app: myteam-app
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
